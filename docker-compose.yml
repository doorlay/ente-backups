services:
  backups:
    build: .
    env_file: .env
    volumes:
      - ${EXPORT_DIR:-/data/ente-photos}:${EXPORT_DIR:-/data/ente-photos}
      - ente-cli-data:/cli-data
      - results:/srv/backups
    restart: unless-stopped

volumes:
  ente-cli-data: {}
  results: {}
